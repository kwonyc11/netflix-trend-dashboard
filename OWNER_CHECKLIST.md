# 오너 확인 체크리스트 — "지금, 이 장르" MVP (index.html)

작성: 프론트엔드 · 2026-07-04 · 대상: 오너 로컬 브라우저 검증

## 0. 실행 방법 (중요)
이 페이지는 `data/*.json`을 **fetch(상대경로)** 로 읽습니다. 따라서 **정적 http 서버로 열어야** 합니다.
파일을 그냥 더블클릭(`file://`)하면 브라우저 보안정책상 fetch가 막혀 데이터가 안 뜨고 콘솔 에러가 납니다.

권장 실행 (프로젝트 폴더에서):
```
python -m http.server 8000
```
→ 브라우저에서 `http://localhost:8000/` 접속.
또는 GitHub Pages 배포본 URL로 접속(백엔드 담당 배포 후).

> 참고: 데이터 파이프라인 정합성(파싱·140슬롯·전 슬롯 insight·poster_url)은 백엔드 `verify_deploy.py`에서 이미 PASS. 아래는 그 위에서 화면/UX를 확인하는 항목입니다.

## 1. 기본 렌더
- [ ] 장르 칩 **14개**가 아이콘과 함께 표시된다 (💥🧭✏️😂🔫🎬🎭👨‍👩‍👧🐉👻🔍❤️🚀🔪)
- [ ] 최초 진입 시 **스릴러(기회 지수 1위)** 가 자동 선택되어 있다
- [ ] 칩을 누르면 아래 3섹션이 해당 장르로 바뀐다 (14개 전부)

## 2. 14개 장르 완주 (완료 기준)
- [ ] **14개 장르 각각**에서 섹션 ①②③가 **빈 값 없이** 렌더된다
- [ ] 어느 장르에서도 "데이터 없음/undefined/NaN" 이 보이지 않는다

## 3. 섹션① 지금 뜨는 것
- [ ] 작품이 순위대로 최대 10개 표시 (rank·제목·연도·⭐평점·투표수)
- [ ] 포스터 이미지가 뜬다 (poster_url). 이미지 없으면 회색 "없음" 플레이스홀더로 폴백
- [ ] `low_vote` 작품에 **"평가 수 적음"** 라벨 표시 (예: 표본 적은 신작)

## 4. 섹션② 먹히는 이유
- [ ] 상위 **3개** 작품만 먼저 보이고, **"더 보기"** 버튼으로 나머지가 펼쳐진다(접기 가능)
- [ ] 리뷰 기반 작품: **녹색 배지**("리뷰 기반 · 긍정 N/M편") + 강점 태그
- [ ] 정량 폴백 작품: **보라 배지**("정량 폴백 · 리뷰 N편"), 강점 태그 없음
- [ ] summary 문장이 항상 표시된다 (프론트는 렌더만, 문장은 JSON 값 그대로)

## 5. 섹션③ 아직 안 한 것 (기회)
- [ ] 등급 배지(tier_label_ko) + 기회 지수(0–100) + gap 헤드라인 + 코멘트 표시
- [ ] `low_confidence=true` 장르(예: SF·다큐)에 **"표본 작아 방향성 참고용"** 주의 문구
- [ ] **"왜?"** 클릭 시 공급 vs 수요 막대 차트가 열리고, **선택 장르 막대가 강조**된다

## 6. 공통 · 출처/한계 (필수 노출)
- [ ] 하단에 데이터 한계 목록(meta.json) 표시
- [ ] **TMDB 귀속 문구** 표시: "This product uses the TMDB API but is not endorsed or certified by TMDB."
- [ ] **비공식 문구** 표시: "본 서비스는 Netflix와 무관한 비공식 프로젝트입니다…"
- [ ] 헤더 워드마크가 **자체 텍스트("NOW 지금, 이 장르")** 이고 넷플릭스 공식 로고를 쓰지 않는다

## 6-1. KPI 애널리틱스 (GoatCounter)
- [ ] 배포 도메인(GitHub Pages)에서 접속 후 대시보드 `https://kwonyoochul.goatcounter.com/` 에 **페이지뷰**가 뜬다 (10초 내)
- [ ] 장르를 바꿔보면 이벤트에 **`briefing-<장르id>`** (도달률)가 집계된다
- [ ] 30초 이상 머무르면 **`engaged-30s`** (체류) 이벤트가 뜬다
- 참고: **localhost/file://에서는 GoatCounter가 기본 필터**하여 집계되지 않습니다(개발 오염 방지). 반드시 배포 도메인에서 확인. 애드블록이 켜져 있으면 집계가 막힐 수 있습니다.

## 7. 품질
- [ ] 개발자도구(F12) **콘솔 에러 0**
- [ ] 모바일 폭(≤600px, DevTools 모바일 뷰)에서 레이아웃 깨짐 없음
- [ ] 키보드 Tab으로 칩/버튼 이동·엔터 동작(접근성)

---
문제 발견 시 장르명·섹션·스크린샷과 함께 알려주시면 바로 수정하겠습니다.
