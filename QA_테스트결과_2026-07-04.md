# QA 테스트 결과 — "지금, 이 장르" MVP

작성: QA/리뷰어 · 2026-07-04 · 대상: `index.html` + `data/*.json` 5종
완료 기준: `OWNER_CHECKLIST.md`, `backend/schema.md`, 노션 Design 백로그(Must)
검증 방식: 코드 레벨 정적 분석 + 원본 JSON 전수 대조. **브라우저 실행 불가 환경** → 런타임 항목은 별도 표기.

---

## 0. 검증 환경 특이사항 (선행 기록)
작업용 Linux 마운트가 `data/*.json` **5개 전부를 멀티바이트(이모지·한글) 경계에서 잘린 스냅샷으로** 읽는 현상 발생(예: `genres.json`을 2,100바이트에서 절단). 호스트 파일 도구로 교차 확인한 결과 **실제 디스크 파일은 완전·유효**했음(genre_count 14 = 항목 14개 일치, opportunity rank 1–14 완결, charts 배열 전부 길이 14). → **마운트 아티팩트로 결론, 실제 결함 아님.** 이후 모든 검증은 권위 있는 호스트 파일 기준으로 수행.

---

## 1. 요약
- **데이터 정합성: 통과(결함 0).** 5개 JSON 전부 파싱 유효·상호 일치. 140개 작품 슬롯, 14개 장르 완결.
- **스키마 계약: 통과.** index.html의 필드 참조가 전부 schema.md 정의 범위 내. null/결측 폴백 구현됨.
- **완료 기준 판정:** 코드·데이터 기준 **치명·높음 버그 0건**. 단 **완료 기준 문서 간 충돌 2건(장르 19종 vs 14종, Must 7 vs 8)** — PM 확정 필요.

---

## 2. 데이터 정합성 대조 (핵심 작업 ①)

### 2-1. 파일별 파싱·구조
| 파일 | 파싱 | 핵심 카운트 | 판정 |
|---|---|---|---|
| genres.json | 유효 | genre_count 14 = genres[] 14 | 통과 |
| trending_by_genre.json | 유효 | 14 장르 × 각 10작품 = 140 슬롯 | 통과 |
| opportunity.json | 유효 | genres[] 14, rank 1–14 완결 | 통과 |
| charts.json | 유효 | labels/genre_ids/series 4종 전부 길이 14 | 통과 |
| meta.json | 유효 | 집계 메타 완결 | 통과 |

### 2-2. trending 필드 전수 카운트 (index.html 렌더 참조 필드)
| 항목 | 실측값 | meta.json 기대값 | 판정 |
|---|---|---|---|
| 총 작품 슬롯(rank) | 140 | rated_titles_n / total_slots 140 | ✅ 일치 |
| 장르당 작품 수 | rank1×14, rank10×14 → 10/장르 | 10 | ✅ 일치 |
| low_vote=true | 10 | low_vote_n 10 (7.1%) | ✅ 일치 |
| low_vote=false | 130 | (140−10) | ✅ 일치 |
| insight.source=reviews | 85 | insight_enrichment.reviews_source 85 | ✅ 일치 |
| insight.source=quantitative | 55 | quantitative_or_pending 55 | ✅ 일치 |
| insight.source=pending | 0 | pending_local 0 (배포 조건) | ✅ 일치 |
| poster_url = 유효 tmdb URL | 140 | (null 0) | ✅ 일치 |
| poster_url = null | 0 | — | ✅ |
| summary 존재 | 140 | 전 슬롯 | ✅ 일치 |
| positive_count 존재(≠null) | 85 | reviews 소스만 | ✅ 일치 |
| positive_count = null | 55 | quantitative 소스만 | ✅ 일치 |
| strengths = [] | 55 | quantitative는 빈 배열 | ✅ 일치 |
| rating_source=imdb | 0 | imdb_fallback_applied false | ✅ 일치 |

### 2-3. opportunity ↔ charts 교차 일치 (전 14장르)
- `charts.genre_ids` 순서 = opportunity rank 1–14 순서(gap 내림차순) — **완전 일치.**
- `charts.series.kr_supply_pct / demand_pct / gap` 각 14값 = opportunity 각 장르 값 — **14/14 일치.**
- **gap = demand_pct − kr_supply_pct** 재계산: 14개 장르 전부 정확(예: 스릴러 13.6−8.6=5.0, 드라마 14.0−20.2=−6.2).

### 2-4. opportunity_index_0_100 산식 검증
문서 산식(`relative_maxabs`, maxabs_gap 6.2) = `round(50 + 50·gap/6.2)` 로 14개 전수 재계산 → **14/14 일치**
(스릴러 90, 액션 82, 어드벤처 70, 가족 60, 공포 57, 판타지 55, 코미디/미스터리 48, 애니 48, SF 47, 범죄 40, 로맨스 37, 다큐 19, 드라마 0).

### 2-5. tier·low_confidence·출처 문구
- tier 라벨 5종 전부 CSS 클래스(`t-strong_opportunity` 등)와 매칭 — 렌더 경로 유효.
- low_confidence=true: SF·다큐멘터리 2개 (OWNER 체크리스트 §5 예시와 일치).
- 푸터 TMDB 귀속 문구·비공식 disclaimer·워드마크 자체 텍스트("NOW 지금, 이 장르") — 코드상 OWNER §6 충족.

**→ 데이터 정합성 결함: 0건.**

---

## 3. 스키마 계약 검증 (작업 ②)
index.html이 참조하는 전 필드가 schema.md 정의 내에 존재. **스키마 외 필드 참조 없음.**

폴백 처리 확인:
- `poster_url` 없음 → 플레이스홀더 노드 + `img.onerror` 이중 폴백. ✅
- `insight` 없음 → `it.insight||{}`, `summary||""`, `strengths` Array.isArray 가드. ✅
- `opportunity_index_0_100 != null` 조건부 렌더. ✅
- 장르별 trending/opportunity 결측 → "데이터 없음" 상태 문구. ✅
- 부트 실패(fetch/parse) → catch 블록에서 정적 서버 안내 에러 표시. ✅

잔여 취약점(데이터로는 미발현): `rating()`/`num()`이 `vote_average`/`vote_count`가 null이면 `NaN` 렌더. 현재 데이터에 null 0건이라 미발현 → 개선 백로그 IMP-3.

---

## 4. 시나리오 정적 분석 (작업 ③) — 14장르 × 3섹션
- **섹션①** `renderTrending`: 장르별 items rank 정렬 → 최대 10행. 14장르 전부 10작품 확보 → 빈 값 없음.
- **섹션②** `renderWhy`: 상위 3 + "더 보기(+N)" 토글(aria-expanded/controls). reviews→녹색 배지+강점태그, quantitative→보라 배지. 14장르 전부 데이터 존재.
- **섹션③** `renderOpportunity`: tier 배지+지수+gap 헤드라인+코멘트, "왜?"→Chart.js 공급vs수요(선택 장르 강조). 14장르 전부 opportunity 레코드 존재.
- 자동 선택: 부트 시 `opportunity.rank===1` = **thriller** → OWNER §1 "스릴러 1위 자동 선택" 충족.

정적 경로상 14×3 전 조합에서 "데이터 없음/undefined/NaN" 미발생(단, 실제 화면 시각 확인은 오너 체크리스트 근거 필요 — 아래 5절).

**런타임 미검증(브라우저 필요, 오너 확인 항목):** 콘솔 에러 0, 모바일 ≤600px 레이아웃, 키보드 Tab/Enter 접근성, GoatCounter 실집계(배포 도메인 한정).

---

## 5. Must 기능 충족/미충족 판정 (작업 ④)

> ⚠️ 완료 기준 문서 충돌: 확정 백로그는 **"MVP = Must 7개"(#1–7)**, #8 "먹히는 이유"는 **Should(실측 조건부 승격)**. PM 작업 지시는 "Must 8개". 아래는 백로그 원문 기준 판정 + #8 별도.

| # | 기능(백로그 원문) | 우선순위 | 판정 | 근거 |
|---|---|---|---|---|
| 1 | 장르 선택 화면 **(19종)** | Must | ⚠️ **조건부 미충족** | 구현 14종. 19종은 meta.json이 "근거 데이터 부재"로 14종 축소·문서화했으나 **백로그 기준(19종)과 불일치**. → I-1 |
| 2 | "지금 뜨는 것" Top10 | Must | ✅ 충족 | 섹션① 14장르×10작품 렌더 |
| 3 | "아직 안 한 것" 기회지수+코멘트 | Must | ✅ 충족 | 섹션③ tier/index/gap/comment |
| 4 | 근거 차트(공급vs수요) | Must | ✅ 충족 | "왜?" → Chart.js, 선택 강조 |
| 5 | 데이터 파이프라인(정적 JSON) | Must | ✅ 충족 | 5 JSON 유효·상호일치 |
| 6 | TMDB 출처+데이터 한계 | Must | ✅ 충족 | 푸터 attribution+limitations+disclaimer |
| 7 | KPI 애널리틱스 | Must | 🟡 코드 충족 / 런타임 미검증 | GoatCounter pageview+briefing-<id>+engaged-30s 코드 존재. 실집계는 배포 도메인 필요 |
| 8 | "먹히는 이유" 리뷰 강점 요약 | **Should(조건부)** | ✅ 기능 구현 / Must 승격은 미확정 | reviews 85 + quantitative 폴백 55. 커버리지 리포트 판정은 "조건부 가능", 승격 결정(a/b) 미기록. → I-2 |

**Must(1–7) 판정: 2–6 완전 충족, 7 코드 충족(런타임 오너검증), 1 조건부 미충족(스코프 충돌).**

---

## 6. 배포 판정

**기술적 결함(코드·데이터) 기준: 치명 0 / 높음 0 → GO 요건 충족.**
데이터 정합성·스키마 계약·렌더 경로 모두 결함 없음. 발견된 버그는 전부 **낮음** (버그 리포트 참조).

**단, 배포 전 PM 확정 필요한 완료 기준 충돌 2건 존재:**
- **I-1 (높음·거버넌스):** 백로그 Must #1 "19종" ↔ 구현 "14종". 문서화된 스코프 축소이나 백로그 미갱신 → 완료 기준 문서 간 충돌. QA가 단독 판단 불가.
- **I-2 (중간·거버넌스):** "Must 8" 지시 ↔ 확정 백로그 "Must 7 + #8 Should". #8 Must 승격 결정 미기록.

### 최종 권고: **조건부 GO**
코드·데이터에 배포를 막는 결함은 없음. **다만 위 I-1(장르 스코프 14종 확정)·I-2(#8 Must 여부)를 PM이 명시적으로 확정한 뒤 배포**할 것을 권고. 이는 코드 결함이 아니라 "완료 기준 정의" 문제이므로, PM 확정 시 즉시 배포 가능.

---

## 7. 산출물
- `QA_테스트결과_2026-07-04.md` (본 문서)
- `버그리포트_2026-07-04.md`
- `개선백로그_초안_2026-07-04.md`
