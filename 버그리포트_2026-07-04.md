# 버그 리포트 — "지금, 이 장르" MVP

작성: QA/리뷰어 · 2026-07-04 · 형식: 재현 절차 / 기대 / 실제 / 심각도

> 정적 분석 + 원본 데이터 전수 대조 기준. **치명·높음 코드/데이터 버그 0건.** 아래는 전부 낮음 등급.
> ※ 완료 기준 문서 충돌(19종/Must 8)은 "버그"가 아닌 거버넌스 이슈 → QA 테스트 결과 §6 및 개선 백로그 참조.

---

## BUG-01 — 섹션② 정량 요약의 평점 소수점 자릿수 불일치
- **심각도:** 낮음
- **재현 절차:** 정량 폴백(quantitative) 작품이 있는 장르 선택 → 섹션① 평점과 섹션② summary 문장의 평점 비교. 예) `Enola Holmes 3`.
- **기대 결과:** 동일 작품의 평점 표기가 두 섹션에서 일관.
- **실제 결과:** 섹션①은 `rating()`으로 소수 1자리(⭐7.1)로 반올림, 섹션② summary는 사전계산 원문에 3자리(`평점 7.063/10`)가 그대로 박혀 노출. 같은 화면에 7.1과 7.063 동시 표기.
- **비고:** 데이터 오류 아님(동일 원본값). 백엔드 summary 생성 시 `round(…,1)` 적용 또는 프론트 정합 규칙 필요. 원천은 `backend/build_data.py`(요약 문자열 생성부).

---

## BUG-02 — 기회 헤드라인 삼항식 중복(양 분기 동일 문자열)
- **심각도:** 낮음
- **재현 절차:** `index.html` `renderOpportunity()` gap-line 생성 코드 확인.
- **기대 결과:** gap 부호에 따라 분기하는 삼항식은 서로 다른 결과를 반환.
- **실제 결과:** `(o.gap>=0?"앞섭니다":"앞섭니다")` — 양 분기가 동일 문자열. 방향어(`dir`)에서 이미 "수요가 공급을/공급이 수요를"을 처리하므로 출력은 정상이나, 삼항식 자체가 죽은 코드.
- **비고:** 출력 결과에는 영향 없음(무해). 가독성·유지보수 위험만. 단순 문자열로 축약 권장.

---

## BUG-03 — 평점/투표수 null 시 "NaN" 렌더 가드 부재
- **심각도:** 낮음 (현재 데이터로 미발현)
- **재현 절차:** `vote_average` 또는 `vote_count`가 null/누락인 작품이 trending에 유입된다고 가정.
- **기대 결과:** "정보 없음" 등 안전한 폴백.
- **실제 결과:** `rating(null)`→`(0.0)`? 실제 `Number(null)=0`이나 `Number(undefined)=NaN` → `NaN` 문자열 렌더 가능. `num(undefined)`도 `NaN`. OWNER 체크리스트 §2 "NaN 미노출" 기준과 충돌 소지.
- **비고:** **현재 데이터에 해당 null 0건 → 실화면 미발현.** 향후 데이터 갱신(주간 자동화 등) 시 회귀 위험 → 방어코드 권장.

---

## 관찰(버그 아님, 참고)
- **OBS-1:** 파비콘이 빨간 라운드 사각형 + 흰 "N" 글자. OWNER §6는 "넷플릭스 **공식 로고** 미사용"만 규정 → 위반 아님. 다만 빨강+N 조합이 상표를 연상시킬 소지 → 상표 리스크 관점 개선 검토(IMP 참조).
- **OBS-2:** poster_url null 0건이라 "회색 없음 플레이스홀더" 경로는 실데이터로 미검증(코드·onerror 폴백은 존재). 배포 후 실제 깨진 이미지로 1회 육안 확인 권장.

---

## 요약
| ID | 심각도 | 상태 |
|---|---|---|
| BUG-01 | 낮음 | 표기 정합 |
| BUG-02 | 낮음 | 죽은 코드 |
| BUG-03 | 낮음 | 방어코드(미발현) |

**치명 0 · 높음 0 · 중간 0 · 낮음 3.** 코드/데이터 기준 배포 차단 버그 없음.
